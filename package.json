{
  "name": "dummy-sensor-server",
  "version": "0.1.0",
  "description": "Server which provides random data",
  "engines": {
    "node": ">6.0.0"
  },
  "main": "./dist/server.js",
  "scripts": {
    "start": "npm run lint && concurrently -k -p -c \"npm run watch\" \"nodemon ./dist/server.js\"",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "npm run lint && npm run build && mocha",
    "eslint": "eslint src",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "lint": "npm run eslint && npm run tslint"
  },
  "author": "Tomi Tuhkanen",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.0.4",
    "@types/express": "^4.0.37",
    "@types/express-serve-static-core": "^4.0.50",
    "@types/mocha": "^2.2.42",
    "@types/node": "^8.0.26",
    "@types/serve-static": "^1.7.32",
    "@types/socket.io": "^1.4.30",
    "chai": "^3.5.0",
    "concurrently": "^3.5.0",
    "mocha": "^2.5.3",
    "nodemon": "^1.9.2",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "cors": "^2.8.1",
    "express": "^4.13.4",
    "json-middleware": "^1.0.2",
    "morgan": "^1.7.0",
    "socket.io": "^1.4.6",
    "superagent": "^3.0.0"
  }
}
